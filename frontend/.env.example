# MeshVault Frontend — Environment Variables
# Copy this to .env and fill in your actual backend URL

# Backend API base URL (REQUIRED for production)
# In development, the Vite proxy handles this — leave blank or omit.
# In production, set to your deployed backend URL (no trailing slash).
VITE_API_URL=https://your-backend-url-here
